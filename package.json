{
  "name": "sistema-buses",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BE,FE -c blue,green \"npm run dev -w backend\" \"npm run start -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.59.0"
  }
}
